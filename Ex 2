#include <stdio.h>
#include <string.h>

int main()
{
    char str[256];
    int a;
    scanf("%s", str);
    a = strlen(str);
    for (int i = a; i >= 0; i--)
    {
        printf("%c", str[i]);
    }
    return 0;
}
