#include <stdio.h>

void swap(int *x, int *y)
{
   int temp;
   temp = *y;
   *y = *x;
   *x = temp;
}

int main()
{
    int x, y;
    scanf("%d", &x);
    scanf("%d", &y);
    swap(&x, &y);
    printf("%d %d", x, y);
    return 0;
}
